%p#notice= notice
%h1#title Tasks
/ scaffoldで自動生成したformを呼び出し
= render 'form', task: @task
%table
  %thead
    %tr
      %th Title
      %th{:colspan => "3"}
  / jQueryでDOMを取得できるようidを付与
  %tbody#task-list
    - @tasks.each do |task|
      %tr
        %td= task.title
        %td= link_to 'Show', task
        %td= link_to 'Edit', edit_task_path(task)
        %td= link_to 'Destroy', task, method: :delete, data: { confirm: 'Are you sure?' }
%br/
